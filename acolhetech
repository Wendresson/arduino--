import streamlit as st
import time

st.set_page_config(page_title="AcolheTech", page_icon="💙")

st.title("💙 AcolheTech - Seu Espaço Seguro")
st.write("Assistente de bem-estar e acolhimento escolar.")

if "messages" not in st.session_state:
    st.session_state.messages = []

# Exibe histórico de mensagens
for message in st.session_state.messages:
    with st.chat_message(message["role"]):
        st.markdown(message["content"])

# Entrada do usuário
if prompt := st.chat_input("Como posso te ajudar hoje?"):
    st.session_state.messages.append({"role": "user", "content": prompt})
    with st.chat_message("user"):
        st.markdown(prompt)

    # Resposta do Robô
    response = f"Olá! Recebi sua mensagem. Vamos conversar sobre como você está se sentindo."
    with st.chat_message("assistant"):
        st.markdown(response)
    st.session_state.messages.append({"role": "assistant", "content": response})
